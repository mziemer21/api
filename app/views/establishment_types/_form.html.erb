<%= form_for(@establishment_type) do |f| %>
  <% if @establishment_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@establishment_type.errors.count, "error") %> prohibited this establishment_type from being saved:</h2>

      <ul>
      <% @establishment_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
